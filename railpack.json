{
  "phases": {
    "extensions": {
      "commands": [
        "install-php-extensions gd ctype curl dom fileinfo filter hash mbstring openssl pcre pdo session tokenizer xml pdo_pgsql redis"
      ]
    }
  }
}
